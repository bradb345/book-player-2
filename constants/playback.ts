// Seconds skipped by the forward/back controls and the lock-screen jump
// buttons. Kept in one place so the UI, the player setup, and the background
// remote handler stay in sync.
export const SKIP_SECONDS = 30;
